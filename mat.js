// const myNumber = -111;
// const output = Math.abs(myNumber);

// const myNumber = 21.6233;
// const output = Math.ceil(myNumber);
// const output = Math.round(myNumber);
// const output = Math.floor(myNumber);// it remove all value after . dot
// const output = Math.floor(Math.random() * 10);

// console.log(output);

for (let i = 0; i <= 30; i++) {
  const output = Math.random() * 2;
  const rounded = Math.round(output);
  console.log(rounded);
}
