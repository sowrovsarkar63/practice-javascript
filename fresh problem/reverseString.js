let gretting = "Hello World ";

// Reverse using prebuilt function

console.log(gretting.split("").reverse().join("🔇"));
/* 
function reversString(str) {
  let reversed = "";

  for (const letter of str) {
    reversed = letter + reversed;
  }
  return reversed;
}

const customizedString = reversString(gretting);
console.log(customizedString);
 */
